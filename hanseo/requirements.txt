pandas
tqdm
torch
transformers
faiss-cpu
sentence-transformers
PyMuPDF
langchain
huggingface_hub
peft

# --- Core HF stack (training & inference) ---
transformers==4.55.*          
datasets>=2.19,<3.0
accelerate>=0.33,<0.35
peft>=0.13,<0.15
safetensors>=0.4.2
tokenizers>=0.15

# --- 4bit(QLoRA) 선택 사용: Linux/x86_64에서만 설치 ---
bitsandbytes>=0.43.0; platform_system == "Linux" and platform_machine == "x86_64"

# --- fine tuning 필요 패키지 ---
chardet>=5.2
Unidecode>=1.3
python-docx>=1.1
textract>=1.6

# --- 일부 모델 토크나이저에 필요 ---
sentencepiece>=0.1.99